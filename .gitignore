# Go
bin/
*.test
*.out
coverage.*
# stray binaries from `go build ./...` in a module dir (named after the dir); the real
# builds go to bin/ via the container
/launcher/gui/gui
/launcher/tui/tui
/menu/example/dmenu/dmenu
/menu/example/wallpaper/wallpaper
/menu/example/wallpaper/gen/gen

# Editors
.obsidian/
.idea/
.vscode/

# OS
.DS_Store

# Release checksums are generated on the release branch (make -f release.mk checksums),
# where the tag covers them. On dev they would churn on every build.
